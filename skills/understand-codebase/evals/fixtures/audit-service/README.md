# Audit service fixture

This synthetic TypeScript service records audit events, exports them asynchronously, and rotates API keys. It is intentionally non-runnable: tests are behavioral documentation for read-only exploration.
